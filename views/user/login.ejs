<%- include('../includes/head.ejs') %>
<body>
    
    <%- include('../includes/navigation.ejs') %>
    <link rel="stylesheet" href="/css/login.css" />


<div class="wrapper fadeInDown">
    <div id="formContent">
      <!-- Tabs Titles -->
  
      <!-- Icon -->
      <div class="fadeIn first">
        <img src="/logo/logo.png" id="icon" alt="User Icon" />
      </div>
  
      <!-- Login Form -->
      <form action="/login" method="POST">
        <input type="text" id="login" class="fadeIn second" name="email" placeholder="Email">
        <input type="text" id="password" class="fadeIn third" name="password" placeholder="password">
        <input type="submit" class="fadeIn fourth" value="Log In">
      </form>
      <!-- Remind Passowrd -->
       <% if (errConfirm) { %>
    <h6 style="color:red;"><%= errConfirm %></h6>
<% } %>

<% if (existUserMsg) { %>
    <h6 style="color: red;"><%= existUserMsg %></h6>
<% } %>

<% if (chngErr) { %>
  <% for(let e of chngErr){ %>
      <h6 style="color: red;"><%= e.msg %></h6>
<% } %>
  <% } %>
      <div id="formFooter">
        <a class="underlineHover" href="/forgot-password">Forgot Password?</a><br>
        <a class="underlineHover" href='/sign-up'>Sign up</a>
      </div>
  
    </div>
</div>

  <%- include('../includes/footer.ejs') %>  